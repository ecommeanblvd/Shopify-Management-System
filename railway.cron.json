{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'cron service: skip Next build (tsx chạy thẳng TS)'"
  },
  "deploy": {
    "startCommand": "npm run cron:sync-orders",
    "cronSchedule": "0 * * * *",
    "restartPolicyType": "NEVER"
  }
}
