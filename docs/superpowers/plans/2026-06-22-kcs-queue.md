# Hàng đợi "Chờ KCS" gộp (hệ #3) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Trang `/f/warehouse/qc` liệt kê mọi đơn vị hàng `qcResult='pending'` across mọi phiếu nhập để KCS làm liền tay, tái dùng form/recordQc hiện có.

**Architecture:** Tách form QC (pass/fail) từ `receiving/[id]` thành action + component dùng chung; thêm query `listPendingQcItems` (gộp FIFO) + trang queue + nav link. Backend `recordQc` giữ nguyên.

**Tech Stack:** Next.js (RSC + server action multipart), Drizzle, S3 signed URL.

## Global Constraints

- **Tái dùng, KHÔNG đổi**: `recordQc`, `uploadReceiptImage`, `decideDisposition`. `recordQc` đã chặn double-QC.
- Trang riêng `/f/warehouse/qc`; sort **FIFO (createdAt asc)**.
- Gate: `view_receiving` (xem), `manage_qc` (hiện action QC).
- Form fail: `reason` + `failPhoto` **required** (giữ nguyên hành vi `[id]`).
- Validate trước push: `npx tsc --noEmit` + `npx vitest run` + `npm run build` xanh.

---

## File Structure
- `features/receiving/qc-actions.ts` — MỚI: `qcPassAction`, `qcFailAction` ('use server', tách từ `[id]`).
- `components/receiving/QcActions.tsx` — MỚI: nút Đạt + form Không đạt (dùng 2 action).
- `app/(dashboard)/f/warehouse/receiving/[id]/page.tsx` — refactor dùng `<QcActions/>`.
- `features/receiving/queries.ts` — thêm `listPendingQcItems()`.
- `app/(dashboard)/f/warehouse/qc/page.tsx` — MỚI: trang queue.
- `app/(dashboard)/f/warehouse/layout.tsx` — thêm nav link "Chờ KCS".

---

## Task 1: Tách QC actions + component (DRY) + refactor [id]

**Files:**
- Create: `features/receiving/qc-actions.ts`
- Create: `components/receiving/QcActions.tsx`
- Modify: `app/(dashboard)/f/warehouse/receiving/[id]/page.tsx`

**Interfaces:**
- Consumes: `recordQc`, `uploadReceiptImage` từ `@/features/receiving/actions`.
- Produces:
  ```ts
  // features/receiving/qc-actions.ts
  export async function qcPassAction(formData: FormData): Promise<void>;
  export async function qcFailAction(formData: FormData): Promise<void>;
  // components/receiving/QcActions.tsx
  export function QcActions({ itemId }: { itemId: string }): JSX.Element;
  ```

Integration/UI — verify tsc/build (không có test thuần mới).

- [ ] **Step 1: Tạo `features/receiving/qc-actions.ts`**

```ts
'use server';
import { revalidatePath } from 'next/cache';
import { recordQc, uploadReceiptImage } from '@/features/receiving/actions';

function revalidateQc(): void {
  revalidatePath('/f/warehouse/qc');
  revalidatePath('/f/warehouse/receiving', 'layout'); // gồm cả receiving/[id]
}

export async function qcPassAction(formData: FormData): Promise<void> {
  await recordQc({ itemId: String(formData.get('itemId')), qcResult: 'pass' });
  revalidateQc();
}

export async function qcFailAction(formData: FormData): Promise<void> {
  const itemId = String(formData.get('itemId'));
  let key: string | null = null;
  const file = formData.get('failPhoto');
  if (file instanceof File && file.size > 0) {
    const fd = new FormData(); fd.set('file', file); fd.set('scope', itemId);
    key = await uploadReceiptImage(fd);
  }
  await recordQc({
    itemId,
    qcResult: 'fail',
    qcFailReason: String(formData.get('reason') ?? ''),
    qcFailPhotoKey: key,
  });
  revalidateQc();
}
```

- [ ] **Step 2: Tạo `components/receiving/QcActions.tsx`**

```tsx
import { Button } from '@/components/ui/button';
import { qcPassAction, qcFailAction } from '@/features/receiving/qc-actions';

/** Nút Đạt + form Không đạt (lý do + ảnh, bắt buộc). Server component — dùng action 'use server'. */
export function QcActions({ itemId }: { itemId: string }) {
  return (
    <div className="flex flex-wrap items-center gap-2">
      <form action={qcPassAction}>
        <input type="hidden" name="itemId" value={itemId} />
        <Button type="submit" size="sm" className="h-7 px-3 text-xs">QC Pass</Button>
      </form>
      <form action={qcFailAction} className="flex items-center gap-2" encType="multipart/form-data">
        <input type="hidden" name="itemId" value={itemId} />
        <input name="reason" placeholder="Lý do fail" required className="border border-input bg-input/30 rounded-md px-2 py-1 text-xs" />
        <input type="file" name="failPhoto" accept="image/*" required className="text-xs" />
        <Button type="submit" size="sm" variant="outline" className="h-7 px-3 text-xs">QC Fail</Button>
      </form>
    </div>
  );
}
```

- [ ] **Step 3: Refactor `receiving/[id]/page.tsx` dùng QcActions**

- Xoá 2 hàm cục bộ `passAction` + `failAction` (dòng ~23-42).
- Bỏ import không còn dùng: nếu `uploadReceiptImage` chỉ dùng trong failAction (đã xoá) thì bỏ khỏi import dòng 7 (giữ `addReceiptItem`, `recordQc` nếu còn dùng chỗ khác — kiểm; `recordQc` giờ không gọi trực tiếp ở page nữa → bỏ khỏi import nếu không nơi nào dùng).
- Thêm import: `import { QcActions } from '@/components/receiving/QcActions';`
- Thay block form QC inline (đoạn `{canQc && it.qcResult === 'pending' && ( <div...><form passAction/><form failAction/></div> )}`) bằng:
```tsx
                {canQc && it.qcResult === 'pending' && <QcActions itemId={it.id} />}
```
> Implementer: chạy `npx tsc --noEmit` sau khi xoá — nếu báo import thừa (`uploadReceiptImage`/`recordQc`/`Button`?), bỏ đúng cái thừa. `Button` có thể vẫn dùng nơi khác trong page — chỉ bỏ khi thực sự không dùng.

- [ ] **Step 4: tsc + build**

Run: `npx tsc --noEmit && npm run build`
Expected: sạch + build OK (`/f/warehouse/receiving/[id]` vẫn build).

- [ ] **Step 5: Commit**

```bash
git add features/receiving/qc-actions.ts components/receiving/QcActions.tsx "app/(dashboard)/f/warehouse/receiving/[id]/page.tsx"
git commit -m "refactor(ops): tách QC actions + QcActions component (DRY) khỏi receiving/[id]"
```

---

## Task 2: Query `listPendingQcItems`

**Files:**
- Modify: `features/receiving/queries.ts`

**Interfaces:**
- Consumes: `signed()` (helper cục bộ sẵn trong file — wrap `getSignedDownloadUrl`).
- Produces:
  ```ts
  export async function listPendingQcItems(): Promise<Array<{
    id: string; unitCode: string; sku: string | null; productTitle: string | null;
    variantTitle: string | null; photoUrl: string | null;
    receiptCode: string | null; sourceType: string | null;
    orderNumber: string | null; brandSlug: string | null; createdAt: Date;
  }>>;
  ```

Integration (db). Verify tsc/build.

- [ ] **Step 1: Thêm query**

Trong `features/receiving/queries.ts`, thêm (cuối file). Kiểm `and`, `eq` đã import từ `drizzle-orm`; dùng `signed()` helper sẵn có trong file:
```ts
/** Mọi đơn vị hàng đang CHỜ KCS (qcResult='pending') across mọi phiếu — FIFO. */
export async function listPendingQcItems() {
  const rows = await db
    .select({
      id: schema.goodsReceiptItems.id,
      unitCode: schema.goodsReceiptItems.unitCode,
      sku: schema.goodsReceiptItems.sku,
      productTitle: schema.goodsReceiptItems.productTitle,
      variantTitle: schema.goodsReceiptItems.variantTitle,
      photoKey: schema.goodsReceiptItems.photoKey,
      receiptCode: schema.goodsReceipts.code,
      sourceType: schema.goodsReceipts.sourceType,
      orderNumber: schema.shopifyOrders.shopifyOrderNumber,
      brandSlug: schema.brandOrderRequests.brandSlug,
      createdAt: schema.goodsReceiptItems.createdAt,
    })
    .from(schema.goodsReceiptItems)
    .innerJoin(schema.goodsReceipts, eq(schema.goodsReceipts.id, schema.goodsReceiptItems.receiptId))
    .leftJoin(schema.shopifyOrders, eq(schema.shopifyOrders.id, schema.goodsReceiptItems.orderId))
    .leftJoin(schema.brandOrderRequests, eq(schema.brandOrderRequests.id, schema.goodsReceiptItems.brandRequestId))
    .where(eq(schema.goodsReceiptItems.qcResult, 'pending'))
    .orderBy(schema.goodsReceiptItems.createdAt);

  return Promise.all(rows.map(async ({ photoKey, ...r }) => ({
    ...r,
    photoUrl: await signed(photoKey),
  })));
}
```
> Implementer: kiểm tên cột thật — `goods_receipts.code` (mã phiếu), `goods_receipts.source_type`, `goodsReceiptItems.createdAt`. Nếu tên khác (vd `receiptCode`/`createdAt` không tồn tại), mở `db/schema.ts` (bảng `goodsReceipts`/`goodsReceiptItems`) khớp đúng tên. `signed` là helper cục bộ trong queries.ts.

- [ ] **Step 2: tsc + build**

Run: `npx tsc --noEmit && npm run build`
Expected: sạch.

- [ ] **Step 3: Commit**

```bash
git add features/receiving/queries.ts
git commit -m "feat(ops): query listPendingQcItems (gộp hàng chờ KCS, FIFO)"
```

---

## Task 3: Trang queue `/f/warehouse/qc` + nav

**Files:**
- Create: `app/(dashboard)/f/warehouse/qc/page.tsx`
- Modify: `app/(dashboard)/f/warehouse/layout.tsx` (nav link)

**Interfaces:**
- Consumes: `listPendingQcItems` (Task 2), `QcActions` (Task 1).

- [ ] **Step 1: Tạo trang queue**

Tạo `app/(dashboard)/f/warehouse/qc/page.tsx` (theo pattern gate của `receiving/[id]` page):
```tsx
import { headers } from 'next/headers';
import { redirect } from 'next/navigation';
import { auth } from '@/lib/auth/auth';
import { getRole } from '@/lib/auth/role';
import { hasPermission } from '@/lib/auth/rbac';
import { listPendingQcItems } from '@/features/receiving/queries';
import { QcActions } from '@/components/receiving/QcActions';
import { Card, CardContent } from '@/components/ui/card';

export default async function QcQueuePage() {
  const session = await auth.api.getSession({ headers: await headers() });
  if (!session) redirect('/sign-in');
  const role = await getRole(session.user.id);
  if (!role || !hasPermission(role, 'view_receiving')) {
    return <div className="px-6 py-16 text-center text-sm text-muted-foreground">Không có quyền.</div>;
  }
  const canQc = hasPermission(role, 'manage_qc');
  const items = await listPendingQcItems();

  return (
    <div className="space-y-6 p-6">
      <div>
        <h1 className="text-2xl font-semibold">Chờ KCS</h1>
        <p className="text-sm text-muted-foreground">{items.length} đơn vị hàng đang chờ kiểm chất lượng (cũ nhất trước).</p>
      </div>
      <Card>
        <CardContent className="p-0">
          <ul className="divide-y divide-border">
            {items.map((it) => (
              <li key={it.id} className="px-5 py-4 flex flex-wrap items-center gap-3">
                {it.photoUrl && <img src={it.photoUrl} alt="" className="h-12 w-12 rounded object-cover" />}
                <div className="min-w-0 flex-1">
                  <div className="text-sm font-medium truncate">{it.unitCode} · {it.sku ?? '—'}</div>
                  <div className="text-xs text-muted-foreground truncate">
                    {it.productTitle ?? ''}{it.variantTitle ? ` · ${it.variantTitle}` : ''}
                    {' · '}phiếu {it.receiptCode ?? '—'}{it.sourceType ? ` (${it.sourceType})` : ''}
                    {it.orderNumber ? ` · đơn ${it.orderNumber}` : ''}{it.brandSlug ? ` · ${it.brandSlug}` : ''}
                  </div>
                </div>
                {canQc && <QcActions itemId={it.id} />}
              </li>
            ))}
            {items.length === 0 && <li className="px-5 py-10 text-center text-sm text-muted-foreground">Không có hàng chờ KCS.</li>}
          </ul>
        </CardContent>
      </Card>
    </div>
  );
}
```
> Implementer: nếu `Card/CardContent` import path khác trong repo, khớp như `receiving/[id]` page. `<img>` đơn giản (ảnh signed S3) — nếu repo dùng next/image ở nơi khác thì vẫn để `<img>` cho ảnh ngoài cho gọn.

- [ ] **Step 2: Nav link "Chờ KCS"**

Trong `app/(dashboard)/f/warehouse/layout.tsx`, mảng nav (chỗ `view_receiving` thêm "Nhập kho & QC"), thêm mục "Chờ KCS":
```ts
    ...(hasPermission(role, 'view_receiving')
      ? [{ href: '/f/warehouse/receiving', label: 'Nhập kho & QC' },
         { href: '/f/warehouse/qc', label: 'Chờ KCS' }]
```
> Implementer: khớp đúng cấu trúc mảng hiện tại (xem dòng 22-23) — chỉ thêm 1 phần tử `{ href: '/f/warehouse/qc', label: 'Chờ KCS' }` vào nhánh `view_receiving`.

- [ ] **Step 3: tsc + build + suite**

Run: `npx tsc --noEmit && npx vitest run && npm run build`
Expected: xanh hết; route `/f/warehouse/qc` xuất hiện trong build output.

- [ ] **Step 4: Commit**

```bash
git add "app/(dashboard)/f/warehouse/qc/page.tsx" "app/(dashboard)/f/warehouse/layout.tsx"
git commit -m "feat(ops): trang Chờ KCS gộp + nav link"
```

---

## Self-Review (đã chạy)

- **Spec coverage:** §4 tách action/component + refactor → Task 1; `listPendingQcItems` → Task 2; trang queue + nav → Task 3. §5 guard (double-QC qua recordQc, required reason/photo trong QcActions, gate) → Task 1/3. §6 (ít logic thuần, integration) → verify tsc/build mỗi task. Đủ.
- **Placeholder scan:** code cụ thể mọi step. Các "Implementer: kiểm tên cột/import" là CHỦ Ý — khớp tên thật, không bịa.
- **Type consistency:** `qcPassAction`/`qcFailAction`/`QcActions({itemId})`/`listPendingQcItems` (shape return) nhất quán giữa task 1/2/3.
- **Lưu ý reviewer:** toàn bộ là UI/query — repo không test DB → gate là tsc/build. `recordQc` (logic QC/disposition) đã có test sẵn, không đụng.
